const { Pool } = require('pg'); const pool = new Pool({ connectionString: 'postgresql://postgres:postgres@localhost:5432/smartvault_db' }); async function test() { const allFiles = await pool.query('SELECT category FROM vault_files LIMIT 5'); console.log('Sample files:', allFiles.rows); const m = await pool.query('SELECT id, name FROM categories LIMIT 5'); console.log('Categories:', m.rows); pool.end(); } test();
